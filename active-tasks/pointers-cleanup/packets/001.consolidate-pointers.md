# Packet: consolidate-pointers
# ID: 001

## Context
## Goal
Consolidate all individual agent pointer files into `pointers.md` and clean up
the repository root.

## Background
We want a clean repository root with only `AGENTS.md` as the main contract.
Individual pointers (CLAUDE.md, GEMINI.md, etc.) make the directory messy.

## Pre-requisites
- write access to repo
- bash

## Plan
1. Create `/pointers.md`.
2. Move the content of all `*.md` files in root (excluding AGENTS.md, README.md,
   LICENSE, and the protocol docs) into `pointers.md` as examples.
3. Add a guide on how to configure agent-specific redirects to `AGENTS.md`.
4. Delete the original pointer files.

## Report Requirements
- `pointers.md` created.
- List of deleted files.
- Doer {Agent Name}.

## Report
<!-- Doer to append report here -->
