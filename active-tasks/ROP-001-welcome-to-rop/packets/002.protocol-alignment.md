# Packet: Protocol Alignment
# ID: 002

## Context
The repository needs to be aligned with the `AGENTS.md` protocol.
Requires access to `active-tasks/` (formerly `tasks/`) and `AGENTS.md`.

## Plan
1. Rename `tasks/` to `active-tasks/`.
2. Create `packets/` directory within the task folder.
3. Update `TASK.md` to include Master Plan, Packet List, and Log.
4. Update `STATUS.md` using the `task-status-update` skill.

## Report Requirements
- Standardized directory structure.
- Updated task ledger and status report.

## Report
- Directory `tasks/` renamed to `active-tasks/`.
- `active-tasks/welcome-to-rop/packets/` created.
- `TASK.md` updated with protocol-compliant sections.
- `STATUS.md` updated with the latest progress.
