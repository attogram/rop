# Packet: execute-cleanup
# ID: 001

## Context
The `active-tasks/` directory is becoming bloated with completed tasks. We need
to move them to a `done-tasks/` directory to maintain high ROPness and a lean
active context.

## Goal
Execute the relocation of ROP-004, ROP-005, and ROP-007 to `done-tasks/` and
update all global status files and README.

## Key Files
- `/active-tasks/STATUS.md`
- `/README.md`
- `/done-tasks/` (to be created)

## Background
The PM (User) suggested moving done tasks to a `done-tasks/` directory to keep
valuable history while reducing FLUFFP of the active state.

## Pre-requisites
- Tasks ROP-004, ROP-005, and ROP-007 must be finalized (🟢).

## Plan
1. Create `/done-tasks/`.
2. Move `/active-tasks/ROP-004-diagnostic-fluff/` to `/done-tasks/`.
3. Move `/active-tasks/ROP-005-pointers-cleanup/` to `/done-tasks/`.
4. Move `/active-tasks/ROP-007-welcome-clarity-and-focus/` to `/done-tasks/`.
5. Update `/active-tasks/STATUS.md`.
6. Update `/README.md`.

## Report Requirements
- List of moved directories.
- Confirmation of link updates in README and STATUS.
- Worker {Agent Name}.

## Report
✅ Summary: Moved ROP-004, ROP-005, ROP-007 to done-tasks/; updated README and STATUS.

- Created `done-tasks/` directory.
- Moved `active-tasks/ROP-004-diagnostic-fluff/` to `done-tasks/`.
- Moved `active-tasks/ROP-005-pointers-cleanup/` to `done-tasks/`.
- Moved `active-tasks/ROP-007-welcome-clarity-and-focus/` to `done-tasks/`.
- Updated `active-tasks/STATUS.md` and `README.md`.
- Verified all markdown links in README.md and active-tasks/STATUS.md.

Worker Jules.
