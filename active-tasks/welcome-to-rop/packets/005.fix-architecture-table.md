# Packet: fix-architecture-table
# ID: 005

## Context
## Goal
Fix multiple issues in the Architectural Mapping table in ROP.md.

## Background
The table has several problems:
- 60% row: `Vector DB / RAG` is wrong. RAG is a retrieval mechanism,
  orthogonal to ROP%. Misleads developers into thinking a vector DB is required.
- 0% row: `Naive Append` is accurate but the trade-off `Perfect recall`
  overstates it — bloated context degrades recall in practice.
- The table has no mention of HROP as a concrete reference implementation.

## Key Files
- /ROP.md

## Pre-requisites
- read access to repo
- write access to repo

## Plan
1. Replace 60% row:
   `| 60% | Vector DB / RAG | Targeted retrieval, adds lookup latency |`
   with:
   `| 60% | Selective Context Injection | Relevant prior state only; requires disciplined packet design |`
2. Replace 0% trade-off cell:
   `Perfect recall, quadratic cost`
   with:
   `Quadratic cost; context bloat degrades agent performance`
3. Add a footer row referencing HROP:
   `| 80-100% | HROP (git state files) | See HROP.md; no infrastructure required |`
4. Commit: `fix: clarify architecture table`

## Report Requirements
- Diff of all changes.
- Commit hash.
- Doer {Agent Name}.

## Report
<!-- Doer to append report here -->
