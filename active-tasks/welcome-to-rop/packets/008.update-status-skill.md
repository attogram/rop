# Packet: Update Status Skill
# ID: 008

## Context
## Goal
Modify the `task-status-update` skill to use a new header format, remove the
Progress section, and add a specific footer.

## Background
The user wants to streamline status reports and ensure they include specific
metadata in the header and footer. The header needs an icon and the exact
directory name.

## Key Files
- /skills/task-status-update/SKILL.md
- /active-tasks/welcome-to-rop/STATUS.md

## Required Skills
- includeFile('skills/prefs-markdown/SKILL.md')

## Plan
1. Update `skills/task-status-update/SKILL.md`:
   - Change the header template to `# {ICON} - {Ticket Number} - {exact name of task directory} - {timestamp}`.
   - Remove the `## Progress` section from the template.
   - Add a footer requirement: `(via {Agents})` preceded by a blank line.
2. Verify the changes in `SKILL.md` adhere to `prefs-markdown` (hard-wrap, header spacing).
3. Update `active-tasks/welcome-to-rop/STATUS.md` to follow this new format immediately.

## Report Requirements
- Copy of the updated `task-status-update` skill template section.
- Verfication that `STATUS.md` has been updated to the new format.

## Report
(Reserved for Doer)
